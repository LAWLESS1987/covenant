<?xml version="1.0" encoding="utf-8"?>
<!-- The actuator's declaration (phase 1). No canPerformGestures: it reads the
     green-lit app's screen and performs node actions (focus, set text, click),
     nothing more. flagRetrieveInteractiveWindows lets it see the app's window. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/actuator_description"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100"
    android:settingsActivity="org.covenant.node.AppsActivity"/>
